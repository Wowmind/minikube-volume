apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvcc
spec:
  storageClassName: demo-storage
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce

